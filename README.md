# 🌄 React + Tailwind + Three.js Mountain Glassmorphism Portfolio

This is a **step-by-step guide** to create a **3D glassmorphism mountain portfolio** using **React, Tailwind CSS, Three.js**, and deploy it live with **Vercel**.

---

## 0️⃣ Prerequisites

Make sure you have the following installed:

- Node.js `>=20.19` or `22.12+`
- VS Code
- Git
- Chrome

Check versions:

```bash
node -v
npm -v
